# Material Harness — CustomersTableComponent

Test CustomersTableComponent using Angular Material CDK test harnesses.

## Spec file

## Key Concepts
- TestbedHarnessEnvironment.loader(fixture) creates the harness loader
- loader.getHarness() retrieves a single harness instance
- MatButtonHarness.with({ text }) filters by button label
- Harnesses provide a stable API that survives DOM structure changes
